require 'spec_helper'

describe GitManageInstall do
  it 'has a version number' do
    expect(GitManageInstall::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
